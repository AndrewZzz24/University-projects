#include<iostream>

int main() {

    freopen("aplusbb.in", "r", stdin);
    freopen("aplusbb.out", "w", stdout);

    long long a, b;

    std::cin >> a >> b;
    std::cout << a + b * b;

    return 0;
}